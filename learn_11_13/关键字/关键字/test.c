#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/*
关键字
	auto			自动变量
	break			跳出循环、case
	case			选择分支
	char			char类型
	const			常属性
	continue		循环
	default			
	do
	double
	else
	enum			枚举
	extern			声明外部符号
	float
	for
	goto			跳转
	if
	int
	long
	register		寄存器关键字
	return
	short
	signed			有符号的
	sizeof			计算大小
	static			静态的
	struct			结构体
	switch
	typedef			类型重命名
	union			联合体
	unsigned		无符号的
	void
	volatile
	while

*/


int main()
{
	auto int a = 10;//auto可以被省略，让局部变量自动创建自动销毁


	return 0;
}